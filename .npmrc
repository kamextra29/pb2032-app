; Ce poste a un omit=dev global (npm config get omit) qui saute les
; devDependencies sur un simple `npm install`. fake-indexeddb (nécessaire à
; test/store.test.mjs) ne serait alors jamais installé. `include=dev` force
; leur installation pour ce projet, quelle que soit la config globale.
include=dev
